---
name: project_economy_plan
description: "Estado del Plan de Mejora Módulo Economía (ECO-0..6) — COMMITEADO en fix/app-improvement-plan; en curso la Propuesta de reestructuración (ECO-2b)"
metadata: 
  node_type: memory
  type: project
  originSessionId: e78f1713-297f-4c72-b8ff-d6c2266c20c0
---

Plan de Mejora — Módulo Economía (Market Intelligence), sprints ECO-0..ECO-6. Al 2026-07-06 **ECO-1..6 implementados** en `AI-Financial-OS/` (backend `app/modules/market_intelligence/`, frontend `apps/desktop/src/features/economy` + `markets`), **sin commitear** (el usuario commitea él tras revisar — ver [[feedback_commits_and_graphify]]).

Verificado: 115 tests `tests/market_intelligence/` en verde; `tsc --noEmit` limpio; overview endpoint 200; UX snapshots headed 21/21. Los 6 fallos del suite backend completo (transactions/investments/ai/price_coverage/backups) son **pre-existentes**, no de ECO (confirmado con git stash).

ECO-6 clave: `GET /api/market-intelligence/economy/overview` colapsa 5 requests en 1 y agrupa por tema en backend (`service.get_economy_overview`); `/ingest-status` cambió a `{current, last_run}` (ECO-5) y el frontend lo normaliza en `useMarketIntelligence.ts` (`normalizeIngestStatus` → VM con `phase`); los dos hooks se unificaron en `useMIResource`. Sin react-query (no se añadió dependencia).

**Hecho después (2026-07-06, sin commit):**
- ECO-5 t4 circuit breaker: implementado en `orchestrator.py` como breaker por-run (contador de fallos por provider, `breaker_threshold=2`, se salta el provider el resto del run y cae a fallback; se resetea por instancia → cada tick horario re-sondea). TTL cache DESCARTADO (redundante con el scheduler por frecuencia). Test `test_breaker_opens_after_repeated_failures_and_resets_on_success`. 116 tests MI verdes.

- Gap INE RESUELTO (2026-07-06, sin commit): el usuario apuntó a la API INE (github es-ine/ineapir → Tempus3). `ine.py` generalizado a mapa `_SERIES` keyed por catalog id (como Eurostat), verificado EN VIVO: `ipc_general`→IPC206449, `desempleo_spain`→EPA86913 (11.76% trimestral, plausible). Bug de raíz arreglado de paso: el parser leía `Periodo` pero Tempus3 devuelve `FK_Periodo` → los periods salían vacíos; añadido `_period_label` (mensual NN / trimestral TN). `pib_spain` AÑADIDO tras verificar en vivo → `CNTR6597` (PIB pm, precios corrientes, CVEC, trimestral), valor crudo 437288 millones€ × scale 1/1000 = 437.3 "EUR bn" (cuadra con la unidad del catálogo; antes lo servía world_bank anual → desajuste de unidad). `_Series` ganó campo `scale`. Los códigos FK_Periodo trimestrales NO son consistentes entre operaciones INE → el periodo se deriva de `Fecha` (epoch) con nudge +12h para absorber desfase Madrid/UTC. Test `test_ine.py` (5). 121 tests MI verdes.

**Expansión INE (2026-07-06, sin commit) — usuario pidió "todos" los dominios:** IPC desglosado, Vivienda/hipotecas, Laboral ampliado, Coyuntura. FLUJO CLAVE confirmado: la UI del overview de Economía es catalog-driven (`get_macro_snapshot` itera lo ingerido; `_region_for` usa `country==ES`→spain; `_THEME_BY_SUBCATEGORY` mapea subcategory→tema). ⇒ añadir indicador al YAML con `provider_primary: ine` + subcategory mapeada = aparece SOLO, sin tocar React. Adapter `supported_indicators` deriva de `_SERIES` (añadir serie = 1 línea). Cada serie se verifica EN VIVO (discovery TABLAS_OPERACION→SERIES_TABLA + readback de valor). Periodos canónicos `YYYY-Qn`/`YYYY-MM` (NO "T"). 
Todos verificados live + smoke OK, 121 tests MI verdes, catálogo 76 (+7 INE). Periodos trimestrales validados contra valores reales (EPA Q3/Q4-2023 = 11.84/11.76 coinciden) → el label por `Fecha`+nudge12h es CORRECTO (FK_Periodo se ignora, es inconsistente).
- DOM1 IPC desglosado DONE: ipc_general→variación ANUAL (IPC290750, 3.2%; antes mensual IPC206449 engañoso — OJO: history vieja en DuckDB es mensual, puede querer purgarse antes de reingerir); +ipc_alimentacion/vivienda/transporte/restauracion (IPC290754/762/774/794, tabla 76125), subcat inflation.
- DOM2 Vivienda DONE (parcial): ipv_spain (IPV948, 12.9%, trimestral, subcat housing→Actividad). HIPOTECAS DIFERIDO: la Estadística de Hipotecas de INE (op.40) no expone serie limpia "sobre viviendas", solo por naturaleza de finca (fincas urbanas/rústicas) → decisión de etiquetado del usuario (usar fincas urbanas como proxy?).
- DOM3 Laboral DONE: coste_laboral_spain (ETCL68, €3.382, trimestral, subcat employment→Empleo).
- DOM4 Coyuntura DONE: comercio_minorista_spain (ICM3431, 5.1%, mensual, subcat consumption→Actividad).
- Método rápido para añadir más series INE: TABLAS_OPERACION/{op|cod} → SERIES_TABLA/{idTabla}?det=2 → DATOS_SERIE/{cod}?nult=1 (readback valor). Tablas grandes dan "Petición en proceso" async → reintentar con requests en Python (bucle), no WebFetch (cachea el processing).

**Propuesta "Reestructuración sección Economía" (doc `docs/# Propuesta — Reestructuración de la sec.md`) — EN CURSO (2026-07-06):**
CORRECCIÓN: ECO-0..6 **ya está commiteado** en `fix/app-improvement-plan` (la nota "sin commit" de arriba era stale). El working tree estaba limpio salvo el doc de Propuesta. Base verificada en código: ine.py `_SERIES`+allowlist, impact.py con `weighted_portfolio_return`/macro_series/constants (ECO-4), routes `/economy/overview` (ECO-6).
Decisiones del usuario para la Propuesta: alcance=ECO-2b (tesoro/ree); gap INE ya resuelto (Tempus); tesoro=Letras+Bonos/Obligaciones.
- **REE (ECO-2b) HECHO:** `ree.py` migrado al contrato estricto (allowlist `precio_electricidad_spain`, `fetch(indicator_id)`). Sirve media MENSUAL del mercado **spot** (pool OMIE, NO PVPC) del mes natural anterior, agregando el horario de `apidatos.ree.es/es/datos/mercados/precios-mercados-tiempo-real?...time_trunc=hour` (endpoint `hour` funciona; `month`/`pvpc/day` dan 400/500). Verificado live: 2026-06 = 69.59 €/MWh. Catálogo `macro_spain.yaml` id `precio_electricidad_spain` subcat `energy` provider_primary ree. Añadido tema **"Energía"** a `_THEME_BY_SUBCATEGORY`+`_THEME_ORDER` en service.py. Test `test_ree.py` (4) verde. SIN COMMIT.
- **Tesoro (ECO-2b) HECHO por scraping** (usuario aprobó la excepción a "scraping fuera de alcance"): el feed JSON commiteado daba 404 (fuente movida) y tesoro.es sirve cadena TLS incompleta (falta intermedio) ⇒ `tesoro.py` reescrito al contrato estricto scrapeando la tabla HTML oficial de `resultado-ultimas-subastas/{letras-del-tesoro|bonos-del-estado|obligaciones-del-estado}` con `verify=False`. IDs: `letras_3m/6m/9m/12m` (plazo fijo, mapeo por "N MESES"), `bono_estado_subasta` + `obligacion_estado_subasta` (plazo ROTA por subasta → se sirve la última = fecha máxima, plazo en el nombre). Parseo: tabla Drupal views (th scope=row=etiqueta, columnas=subastas); fila "Tipo de interés marginal" (evitar "Anterior tipo marginal"); coma decimal; label de plazo RECONSTRUIDO desde el entero+MES/AÑO (la Ñ de "AÑOS" viene corrupta en origen pese a charset utf-8). Verificado live: L3M 2.244%, L12M 2.567%, bono 5A 2.84%, oblig 10A 3.161%. Catálogo: 6 ids subcat `interest_rates` provider_primary tesoro. Test `test_tesoro.py` (5, fixture HTML sin red).
- **Comparativas Nivel-1 HECHAS (2 de 3)** en `impact.py` `_build_comparatives` (bloques imperativos — NO hay tabla de definiciones; ECO-4 refactorizó lecturas/ponderación/buckets pero las comparativas siguen hardcodeadas, la Propuesta asumía tabla que no existe):
  - `electricity_pool_vs_home` (#7c): pool `precio_electricidad_spain` vs gasto Casa (`home_monthly`), yoy con umbrales `C.ENERGY_YOY_UP/DOWN`; degrada a neutral/no_data sin histórico. Coexiste con la 7b (proxy Brent para gas/combustible).
  - `letras_vs_savings` (#1b): `letras_12m` vs mejor tipo remunerado del usuario. Nuevo helper público `savings_service.current_annual_rate(db, config)` (envuelve `_annual_rate_for`, INV-4); `impact._best_savings_rate(db)` = max entre SavingsAccountConfig. Solo si el usuario tiene cuenta remunerada. Cuantifica €/año si Letras rinden más. Import cruzado impact→investments OK (sin ciclo).
  - Test `test_impact_electricity.py` (8, función pura con dicts). Estas comparativas fluyen a `/economy/overview` vía compute_personal_impact SIN tocar React.
- Estado tests: **139 MI verdes** + 6 savings/investment. Sin regresiones. TODO SIN COMMIT (pendiente revisión+commit del usuario).
- **Comparativa Nivel-1 #3 "inflación de tu cesta" HECHA (2026-07-07, sin commit):** bloque `basket_inflation` en `impact.py` (tras #1). IPC ponderado por gasto real: subgrupos IPC ya en catálogo (ipc_alimentacion/vivienda/transporte, DOM1) × buckets `food_home_monthly`/`home_monthly`/`transport_monthly`; el gasto restante o sin subgrupo se imputa a `ipc_general`. `personal_infl = (Σ gasto·sub + resto·ipc_general)/gasto_total`. compute_signal(higher_is_better=False) vs ipc_general; cuantifica €/mes extra. Solo si hay gasto clasificado (>0) e `ipc_general`. Añadidos ipc_alimentacion/vivienda/transporte a `_MI_EMPTY` + `_get_mi_data`. 3 tests en test_impact_electricity.py. Las 3 comparativas Nivel-1 de la Propuesta están COMPLETAS.
- **Estructura UI 3 niveles + fix policy_rate HECHOS (2026-07-07, sin commit):** taxonomía de temas §3 en `service.py` `_THEME_BY_SUBCATEGORY`/`_THEME_ORDER` → 6 temas España ("Precios y consumo", "Vivienda", "Ahorro y tipos", "Empleo y salarios", "Energía", "Actividad y cuentas públicas"); `policy_rate`→"Ahorro y tipos" (fix del bug "Otros"); `monetary`/`sentiment`/`pmi` reubicados; "Confianza y PMI" eliminado. `mock-data.ts` alineado. `EconomyPage.tsx` reescrito en 3 niveles: **Nivel 1 «Tu economía»** (PersonalEconomySection + comparativas, subidas desde el fondo a protagonista), **Nivel 2 «España por temas»** (ES siempre visible, sin tabs, primary/secondary + "mostrar más"), **Nivel 3 «Eurozona y EEUU»** (compacto, tab EA/US limitado). RatesAndDebt baja a contexto de mercado al final. Helpers `ThemedGroups`+`LevelHeader`. `tsc --noEmit` limpio; agrupado verificado (policy_rate→Ahorro y tipos). Ningún test atado a nombres de tema.
- **Pendiente de la Propuesta (NO hecho):** triage §6 (seguridad_social tier2, descartar aemet/agencia_tributaria de UI, IPC por CCAA/sector tier2 — decisiones de catálogo); hipotecas INE (DOM2, etiquetado). PENDIENTE ux:snapshots:headed (regla) + revisión/commit del usuario.

**Fixes UI Economía (2026-07-07, sin commit) — feedback del usuario sobre capturas:**
- **"Tipos y deuda" siempre vacío** (Bono España 10A / Bund 10A / Prima de riesgo = "—"): causa raíz de DATOS, no de front. `spain_10y` lo bajaba `bde` como MacroIndicator (campo `value`, no `yield_value`) → no entraba en bond snapshot; `germany_10y` NINGÚN adapter lo servía (ecb lo rechaza, fred no lo tenía). FIX: añadidos a `fred._CATALOG_TO_FRED_SERIES` (`spain_10y`→IRLTLT01ESM156N, `germany_10y`→IRLTLT01DEM156N, ambos "10Y", vía OCDE en FRED) + `_YIELD_COUNTRY` (ES/DE) para no etiquetarlos US; `_parse_yield_csv` ahora acepta `country` y lee `observation_date` (FRED cambió el header de `DATE`). `bonds.yaml`: provider_primary de ambos → `fred` (secundario bde/ecb). Verificado live: ES 3.48%, DE 3.05%, prima ≈44bps. Storage casa por maturity (`_expected_maturity`), country no bloquea.
- **Flecha ▲/▼ subida/bajada**: ya existía en `IndicatorCard` pero gris minúscula; ahora chip con color (emerald sube / rose baja) y `text-sm`. Solo aparece con ≥2 periodos de histórico en DB (delta vs periodo anterior en service.py L104-106); poblará según acumule histórico.
- **Texto muy pequeño**: subidos tamaños en `IndicatorCard` (título text-sm/base, valor text-2xl/3xl, periodo text-xs), `RatesAndDebtSection` Tile (label text-sm, valor text-2xl), y headers de `EconomyPage`+secciones (`text-caption`/`text-[11px]` → `text-sm`). `tsc --noEmit` limpio. PENDIENTE: ux:snapshots:headed (regla del usuario) antes de cerrar.

**Decisiones abiertas / no auto-implementables (para el usuario):**
- ECO-3b: migración almacén DuckDB→SQLite WAL — gated a aprobar el ADR `docs/internal/eco3_adr_storage_engine.md`. NO hacer unilateral.
- Tipos BCE: RESUELTO en el ADR anexo A2 → **no se unifican** (`ReferenceRateObservation` es fuente de cálculo, MI es presentación). Sin acción.
- ECO-6 t4: defectos visuales concretos — pendiente de capturas concretas.


---
**Relacionadas:** [[project_investments_module]] · [[project_markets_module]] · [[project_constraints]] · [[feedback_commits_and_graphify]]

Tags: #módulo #plan #decisión
